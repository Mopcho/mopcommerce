import { Request, Response } from 'express';

export async function getUsers(req: Request, res: Response) {
	try {
        
	} catch (err) {
		console.log('Controller error : [getUsers] : ', err);
	}
}
